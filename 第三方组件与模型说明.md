# 第三方组件与模型说明

本文件用于说明 DouyinFilter Android V0.7.5 所依赖的主要第三方模型和软件组件。

本项目自己的 MIT License 只覆盖本项目作者有权授权的原创代码，不自动覆盖第三方模型权重、第三方库、论文、商标和其他材料。

## 一、SigLIP2 显式内容分类模型

使用模型：

`prithivMLmods/siglip2-x256p32-explicit-content`

当前构建使用：

`checkpoint-1656`

用途：读取图像分类结果中的 `Enticing or Sensual` 类别，用于多帧视觉内容判断。

该模型是基于 Google SigLIP2 系列基础模型进一步微调得到的第三方模型。

Hugging Face 模型页面当前标注许可证为：

`Apache-2.0`

项目地址：

`https://huggingface.co/prithivMLmods/siglip2-x256p32-explicit-content`

使用和再分发时，请以该模型原始仓库最新公布的 License、Model Card 和使用要求为准。

## 二、OpenAI CLIP

使用模型：

`openai/clip-vit-base-patch32`

用途：在构建阶段预先计算女性、男性、无人场景的文本特征，并在 Android 端使用视觉分支进行辅助人物主体判断。

OpenAI CLIP 官方代码仓库许可证为：

`MIT License`

官方项目：

`https://github.com/openai/CLIP`

模型页面：

`https://huggingface.co/openai/clip-vit-base-patch32`

本项目不声称拥有 OpenAI CLIP 模型、论文、商标或相关材料的知识产权。

## 三、ONNX Runtime Android

本项目通过 ONNX Runtime Android 在手机本地运行 FP16 ONNX 模型。

项目：

`https://github.com/microsoft/onnxruntime`

实际使用版本以 `app/build.gradle` 为准。

使用、修改和再分发时，请遵守 ONNX Runtime 自身许可证。

## 四、Android / Gradle / GitHub Actions 相关组件

项目构建还会使用 Android SDK、Gradle、GitHub Actions、PyTorch、Transformers、ONNX、Hugging Face Hub 等第三方工具和依赖。

这些组件均保留各自原始许可证和权利声明。

## 五、商标与平台关系

“抖音”“字节跳动”“OpenAI”“Google”“Hugging Face”“Microsoft”等名称和商标归各自权利人所有。

本项目与上述公司、平台或模型作者不存在官方合作、授权、背书或隶属关系。

## 六、使用提醒

第三方模型和依赖的许可证、Model Card、使用政策可能更新。本文件仅记录当前项目采用时的主要信息，不能替代原权利人发布的正式许可证文本。

在复制、修改、发布、打包 APK 或再分发模型时，请自行核对相应第三方项目的最新授权条款。
